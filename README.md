# tintuc-web-springboot-react
A modern news website built with Spring Boot (REST API) + React Vite (Frontend) + Supabase (Database &amp; Auth). Supports CRUD articles, category filter, authentication, image upload and real-time updates.
